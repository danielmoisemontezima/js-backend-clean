{
  "name": "clean-api",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "migrate:up": "npm run build && node dist/infrastructure/database/migrationRunner.js up",
    "migrate:down": "npm run build && node dist/infrastructure/database/migrationRunner.js down",
    "seed:up": "npm run build && node dist/infrastructure/database/seedRunner.js up",
    "seed:down": "npm run build && node dist/infrastructure/database/seedRunner.js down"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^25.0.3",
    "@types/pg": "^8.16.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.16.3",
    "zod": "^4.3.5"
  }
}
